<!DOCTYPE html>
<html lang="en">
<head>
    {{>header}}
    <title>Manage Items</title>
</head>
<body>
    <h2>Manage Items</h2>
    <ul>
        {{#items}}
        <li>
            <div>
                <h4>{{food}}</h4>
                <p>Quantity: {{quantity}}</p>
                <p>Expiry: {{expiry}}</p>
                <a href="/admin/items/update/{{food}}">Edit</a> | 
                <form action="/admin/items/delete/{{food}}" method="post" style="display: inline;">
                    <button type="submit" onclick="return confirm('Are you sure you want to delete this item?');">Delete</button>
                </form>
            </div>
        </li>
        <hr>
        {{/items}}
    </ul>
    <a href="/newItem">Create New Item</a>
</body>
</html>
